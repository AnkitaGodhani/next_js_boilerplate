/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  output: "export",
  // i18n: {
  //   defaultLocale: "en",
  //   locales: ["en", "de", "gu", "fr"],
  // },
  images: { unoptimized: true },
};

export default nextConfig;
